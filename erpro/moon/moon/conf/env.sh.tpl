#/bin/bash

# 游戏运行时环境变量(由工具生成)

TAG=${tag}
IP=${ip}
HOST=${host}
DIR_ROOT=${game_dir}
ERL=erl
ERL_COOKIE=${erl_cookie}
ERL_PORT_MIN=40001      # erl节点间通讯端口
ERL_PORT_MAX=40100      # erl节点间通讯端口
PORT=${game_port}
ERL_DEBUG=${erl_debug}

